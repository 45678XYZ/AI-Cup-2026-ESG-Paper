name: aicup-esg-paper
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch=2.2.*
  - pytorch-cuda=12.1
  - pip
  - pip:
      - transformers==4.40.2
      # Required when DeBERTa-v3's SentencePiece tokenizer is converted to
      # the fast tokenizer used by AutoTokenizer.
      - protobuf==4.25.3
      - scikit-learn>=1.3.0
      - pandas>=2.0.0
      - numpy>=1.24.0,<2.0
      - sentencepiece
      - safetensors
      - pytest
