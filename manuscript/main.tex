\documentclass[sigconf,article]{acmart}
\settopmatter{printacmref=false}
\renewcommand\footnotetextcopyrightpermission[1]{}

\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{placeins}
\newfontfamily\cjkfont[Path=fonts/]{DroidSansFallback-ai-cup-subset.ttf}

% Nine floats on eight pages. At the class defaults LaTeX refuses to put more
% than 70% of a page into floats and insists on 20% running text, which on a
% page carrying two tables leaves a band of whitespace it will not fill. These
% are the standard relaxations; they change placement only, never content.
\renewcommand{\topfraction}{0.92}
\renewcommand{\bottomfraction}{0.75}
\renewcommand{\textfraction}{0.07}
\renewcommand{\floatpagefraction}{0.78}
\setcounter{topnumber}{3}
\setcounter{totalnumber}{4}

% Hugging Face identifiers are long, contain slashes and hyphens, and TeX will
% not break them: set as-is they run several centimetres past the column.
% \path is the url package's breakable verbatim, which is exactly this problem.
\newcommand{\hfid}[1]{{\small\path{#1}}}

\begin{document}

\title[Hierarchical Projection as a Validity Layer]{Team\_10537 at the NTCIR-19
  AI CUP-VeriPromiseESG Task:\texorpdfstring{\\}{ }Hierarchical Projection as a
  Validity Layer\texorpdfstring{\\}{ }for Multilingual ESG Promise Verification}

\input{metadata}

\begin{abstract}
\emergencystretch=1em
ESG promise verification requires jointly predicted fields to satisfy hard
parent--child constraints, particularly when outputs are intended for
audit-oriented use. We study hierarchical projection as a retraining-free
validity layer for field-wise classifiers. Seven decision rules are compared
on identical cross-fitted probabilities and test rows in Chinese, followed by
projection-versus-argmax replication across English, French, Japanese, and
Korean corpora. Projection reduces the Chinese invalid-tuple rate from
12.55\% to zero by construction. Its official weighted macro-F1 contrast is
undetected ($\Delta=-0.001$, $p_{\mathrm{Holm}}=1.000$), while pre-specified
tuple accuracy improves by $+0.035$ ($p_{\mathrm{Holm}}=.001$). Across 32
external backbone-by-loss arms, tuple accuracy improves in all 32, whereas
weighted macro-F1 improves in 14. These results position hierarchical
projection not as a guaranteed route to higher field-wise scores, but as a
practical way to guarantee coherent outputs and repeatedly improve
complete-output correctness, with field-score trade-offs varying across
models and corpora. Validity alone does not settle which structured rule to
use: searching all 17 legal states instead of projecting greedily costs
$-0.006$ tuple accuracy ($p_{\mathrm{Holm}}=.028$), an adverse pre-specified
result we report in full. Code and reproducibility artifacts are available at
\repourl.
\end{abstract}

\keywords{ESG, promise verification, hierarchical classification,
  constrained decoding, evaluation metrics}

\maketitle
\fancypagestyle{plain}{%
  \fancyhf{}%
  \renewcommand{\headrulewidth}{0pt}%
  \renewcommand{\footrulewidth}{0pt}%
  \fancyfoot[C]{\normalfont\normalsize\thepage}%
}
\pagestyle{plain}
\thispagestyle{plain}

% AI CUP organizers confirmed these fields for the NTCIR-19 special session.
\section*{Team Name}
\teamname

\section*{Subtasks}
\subtasks

\input{sections/01_introduction}
\input{sections/02_related_work}
\input{sections/03_task_and_data}
\input{sections/04_methods}
\input{sections/05_experiments}
\input{sections/06_results}
\input{sections/07_discussion}

\bibliographystyle{ACM-Reference-Format}
\begingroup
\Urlmuskip=0mu\relax
% Keep numeric URL leaves with their path while retaining other local breakpoints.
\def\UrlBreaks{\do\.\do\@\do\\\do\!\do\_\do\|\do\;\do\>\do\]\do\)\do\,\do\?\do\'\do\+\do\=\do\#\do\-}
\bibliography{references}
\endgroup

\end{document}
