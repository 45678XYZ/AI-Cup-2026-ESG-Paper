\section{Results}

The results compare official field-wise scoring with structure-aware analyses;
the latter are post hoc evaluation measures.

\begin{table}[t]
  \centering
  \caption{Comparison of the two evaluation estimands.}
  \label{tab:regimes}
  \resizebox{\columnwidth}{!}{\tiny\input{../tables/table3_regimes.tex}}
\end{table}

\begin{table*}[t]
  \centering
  \scriptsize
  \caption{All pre-specified contrasts across four metric families.}
  \label{tab:contrasts}
  \begingroup
  % Narrow intercolumn spacing before measuring the generated tabular, then
  % scale it to the full-width float without changing its numeric body.
  \setlength{\tabcolsep}{1pt}
  \resizebox{\textwidth}{!}{\tiny\input{../tables/table4_contrasts.tex}}
  \endgroup
\end{table*}
